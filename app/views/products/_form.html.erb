<%= form_with(model: product) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :presentation %>
    <%= form.text_field :presentation %>
  </div>

  <div class="field">
    <%= form.label :concentration %>
    <%= form.text_field :concentration %>
  </div>

  <div class="field">
    <%= form.label :controlled %>
    <%= form.check_box :controlled %>
  </div>

  <div class="field">
    <%= form.label :classification_id  %>
    <%= form.collection_select :classification_id, Classification.order(:description), :id, :description %>
 </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
